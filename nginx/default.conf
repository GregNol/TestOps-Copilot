upstream api_backend {
    server api:8080;
}

upstream llm_backend {
    server llm:8081;
}

upstream frontend_server {
    server frontend:8082;
}

# HTTP сервер
server {
    listen 80;
    server_name testops-cloud.ru;

    client_max_body_size 100M;

    # Логи
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # API запросы проксируем на backend
    location /api {
        # Проксируем на LLM сервис через upstream в docker сети
        proxy_pass http://llm_backend/api;
        proxy_http_version 1.1;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Увеличиваем таймауты для LLM запросов
        proxy_connect_timeout 300;
        proxy_send_timeout 300;
        proxy_read_timeout 300;
        send_timeout 300;
        
        # WebSocket support (если нужно)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Все остальные запросы - на фронтенд
    location / {
        proxy_pass http://frontend_server;
        proxy_http_version 1.1;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Кэширование статики
        proxy_cache_bypass $http_upgrade;
    }
}


# HTTPS сервер (опционально, если нужны сертификаты)
# server {
#     listen 443 ssl http2;
#     server_name testops-cloud.ru;
#
#     ssl_certificate /etc/nginx/ssl/live/testops-cloud.ru/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/live/testops-cloud.ru/privkey.pem;
#
#     client_max_body_size 100M;
#
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     location /api/ {
#         proxy_pass http://api_backend;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_connect_timeout 300;
#         proxy_send_timeout 300;
#         proxy_read_timeout 300;
#         send_timeout 300;
#     }
#
#     location / {
#         proxy_pass http://frontend_server;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
