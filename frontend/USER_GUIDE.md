# TestOps-Copilot Frontend - Новый Workflow

## 🎯 Что изменилось

Фронтенд полностью переработан для интеграции с LLM backend API и использует **state machine** для управления пользовательским потоком.

## 🚀 Быстрый старт

### Выбор режима тестирования

При создании нового чата появится диалог выбора режима:
- **UI Testing** - для тестирования веб-интерфейсов
- **API Testing** - для тестирования API на основе OpenAPI спецификации

### UI Testing Workflow

1. **Описание проекта**
   ```
   Тестирование сайта https://example.com
   Основной функционал: авторизация, каталог товаров, корзина
   Модули: Auth, Products, Cart
   ```

2. **Получение тест-плана** (таблица в Markdown)
   - Бот сгенерирует план тестов

3. **Редактирование плана** (опционально)
   ```
   Добавь тесты на фильтрацию товаров
   Убери тесты на админ-панель
   ```

4. **Утверждение плана**
   ```
   готово
   ```
   или
   ```
   генерируй код
   ```

5. **Получение Python кода**
   - Бот сгенерирует pytest код

### API Testing Workflow

1. **Загрузка спецификации**
   - Прикрепите файл OpenAPI (JSON/YAML)
   - Добавьте описание: `Тестирование REST API для интернет-магазина`

2. **Получение тест-плана**
   - Бот проанализирует спецификацию и создаст план

3. **Редактирование плана** (опционально)
   ```
   Убери тесты на эндпоинты /internal/*
   Добавь негативные тесты для валидации
   ```

4. **Утверждение и генерация кода**
   ```
   ок
   ```

## 📊 Индикатор прогресса

В верхней части чата отображается текущий шаг:

- 🎯 **Шаг 1**: Опишите проект для генерации тест-плана
- 📝 **Шаг 2**: Редактируйте план или напишите "готово" для генерации кода
- ⚙️ Генерация кода...
- ✅ Работа завершена!

## 🎨 Новые возможности

### Визуальные улучшения

- ✨ Современный дизайн с градиентами
- 🌓 Тёмная/светлая тема (переключатель в сайдбаре)
- 💬 Сообщения в виде речевых пузырей
- 📝 Inline редактор JSON/YAML
- 🎯 Индикатор текущего шага workflow

### UX улучшения

- 📂 История чатов в сайдбаре
- 🔖 Автоматические заголовки для чатов
- 💾 Сохранение в localStorage
- ⚡ Подсказки после каждого шага
- 🚫 Защита от пропуска шагов workflow

## 🛠️ Технические детали

### API Endpoints

Все запросы идут через прокси `http://localhost:8080/api/v1/ai/{endpoint}`:

- `GET /health` - health check
- `POST /generate-ui-tests` - генерация UI тестов
- `POST /generate-api-tests` - генерация API тестов (multipart/form-data)
- `POST /redact-content` - редактирование контента
- `POST /generate-code-pytest` - генерация Python кода
- `POST /optimize-tests` - оптимизация тестов
- `POST /review-code` - ревью кода

### State Machine

```
idle → generate-ui/generate-api → [редактирование loop] → generate-code → complete
```

Workflow state хранится в Pinia store и синхронизируется с localStorage.

### Ключевые слова для утверждения

Бот распознаёт следующие фразы как подтверждение плана:
- "готово"
- "ок" / "да"
- "утверждаю" / "одобряю"
- "генерируй код" / "давай код"

## 📝 Примеры использования

### Пример 1: UI тестирование

```
Пользователь: https://shop.example.com - интернет-магазин
Описание: каталог, корзина, оплата
Модули: Catalog, Cart, Payment

Бот: [генерирует таблицу с тест-кейсами]

Пользователь: Добавь тесты на промокоды

Бот: [обновляет таблицу]

Пользователь: готово

Бот: [генерирует Python код]
```

### Пример 2: API тестирование

```
Пользователь: [прикрепляет openapi.yaml]
API для управления заказами

Бот: [анализирует спецификацию, генерирует план]

Пользователь: Убери тесты на /admin

Бот: [обновляет план]

Пользователь: ок

Бот: [генерирует код]
```

## 🐛 Troubleshooting

### Backend недоступен

Если видите ошибку "Сервер недоступен":
1. Проверьте что docker контейнеры запущены: `docker compose ps`
2. Проверьте health: `curl http://localhost:8080/api/v1/ai/health`
3. Проверьте логи: `docker compose logs llm`

### Workflow не переключается

Если застряли на одном шаге:
- Создайте новый чат (кнопка "+ Новый чат")
- Очистите localStorage: F12 → Application → Local Storage → Clear

### Файл не загружается

Для API тестирования поддерживаются только:
- JSON файлы (`.json`)
- YAML файлы (`.yaml`, `.yml`)

## 📚 Дополнительная информация

Подробная техническая документация: [WORKFLOW.md](./WORKFLOW.md)
